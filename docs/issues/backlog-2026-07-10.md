# Backlog — pre-existing TODOs, organized as issues (2026-07-10)

Pulled together from scattered "Not done" / "Future ideas" / "Known gaps" / "Watch out for" notes across
`memory/project_state.md` (all sessions through 2026-07-10), written up as proper issues now that GitHub Issues
are unavailable (see `docs/agents/issue-tracker.md`). Numbering continues from
`docs/qa/2026-07-10-pixel8-qa-notes.md` (issues 1-8); see `docs/issues/README.md` for the combined index.

Excluded on purpose: pure style/lint nits from the 2026-07-09 inspection-report triage that the user already
explicitly decided to leave alone (`MayBeConstant`, `SameParameterValue`, unused-string-resource warnings, etc. —
`project_state.md` line ~221, "no behavior impact, high volume for low value, left alone"). Those aren't
backlog, they're closed decisions.

---

## 9. [High] Task C (per-context stream policy) code is unverified — audit before building on it — AUDITED, clean

**Verdict: safe to build on, no rework needed.** Traced all five settings (`gridStreamType`,
`fullscreenStreamType`, `preferSubStreamGrid`, `preferSubStreamFullscreen`, `keepOffscreenTilesAlive`)
end-to-end from `UserSettingsRepository` DataStore key → `SettingsViewModel` → real Settings UI control →
`CamerasViewModel`/`CamerasScreen` — each one has a real default, a real UI control, and a real, traced effect
on grid/fullscreen rendering decisions (not just "the word appears in the file"). No synthetic/fake data, no
emulator-only special-casing, and the `_sub` live-restream-only rule is respected (VOD/clip paths never see it).
Unlike Task B's ICE-candidate/cleartext hacks, this code is genuinely solid.

**One unrelated finding, tracked separately**: `AndroidManifest.xml` (`usesCleartextTraffic="true"`) and
`network_security_config.xml` still have cleartext traffic enabled app-wide — this is Task B's leftover flip
(not something Task C introduced or depends on) and hasn't been reverted. Worth a follow-up cleanup pass; not
blocking Task C work.

<details>
<summary>Original audit request (superseded by the verdict above)</summary>

**Context**: A concurrent tool (not this assistant) that was fixing an unrelated black-screen bug on 2026-07-10
also added `gridStreamType`/`fullscreenStreamType`/`preferSubStreamGrid`/`preferSubStreamFullscreen`/
`keepOffscreenTilesAlive` to `UserSettingsRepository`/`SettingsViewModel` as a side effect. That same tool's other
changes in the same window turned out to be broken (synthetic ICE candidates that made WebRTC always fail, a
forced-RTSP-failure emulator hack, cleartext traffic flipped app-wide) — all found and cleaned up in the
"Post-Task-B cleanup" session. **Nobody has checked whether the Task C settings code has the same kind of
problem.**

**Why it matters**: Task C (per-context stream policy — letting grid vs. fullscreen have independent
stream-type/sub-stream preferences) is real, wanted scope — but building on top of unverified code from a source
that already caused real damage once in this repo is risky.

**Definition of done**: Read through the Task C settings plumbing end-to-end, confirm it's either (a) correct and
usable as a foundation, or (b) needs to be torn out and redone like the WebRTC/RTSP hacks were. Don't add new
Task C features on top of it until this check happens.

</details>

---

## 10. [Medium] Real-device verification checklist — now that a Pixel 8 is available over ADB

Several features were implemented and marked "done" based on **emulator-only** verification, with real-hardware
testing explicitly flagged as outstanding. Bundled here since they're all the same kind of task and the user now
has real Pixel 8 access:

- **Preview-frame thumbnail scrubbing** (timeline scrub bubble) — visually confirmed on-device via emulator +
  one user-provided real-device screenshot mid-development, but never a full on-device pass on the real Pixel 8.
- **Picture-in-Picture auto-trigger on home-press** (`onUserLeaveHint()`) — only the explicit in-app "Enter PiP"
  button was tested; the automatic trigger on pressing Home/Recents while a camera is focused was wired but never
  separately exercised.
- **`TokenRefreshAuthenticator` live-401 recovery** — the authenticator is wired into `FrigateClient` and smoke
  tested (cameras/events/event-detail all still load), but the actual "receives a real 401, silently refreshes,
  retries" path was never forced/observed live (would need a way to expire a real JWT against the test server).
- **Fullscreen back-handling, narrow path** — verified with `autoLandscapeOnStream` ON (a single Back exits both
  fullscreen and the focused view together). The `autoLandscapeOnStream` OFF path (first Back only collapses
  fullscreen, second Back leaves) compiled but was never separately screenshotted/exercised. Code is symmetric
  between RTSP/WebRTC so it should hold — flag if a real repro says otherwise.

**Definition of done**: each bullet gets an explicit on-device pass on the real Pixel 8, with a note on what was
observed (pass/fail) added back to `project_state.md`.

---

## 11. [Low/Medium] Event card shows a nonsensical duration ("170:44") — FIXED

**Root cause**: `formatEventTime()` in `EventsScreen.kt` computed the duration pill as raw
`String.format("%d:%02d", secs / 60, secs % 60)` — minutes never rolled into hours, so a 2h50m44s event
(10,244s) rendered as "170:44" instead of "2:50:44".

**Fix**: duration now branches on whether the event is >= 1 hour — `H:MM:SS` if so, `M:SS` otherwise (unchanged
for the common case).

**Verified on emulator**: Events grid still renders short durations correctly ("16:07", "0:05", "8:20", "21:24"
all still M:SS, no regression). Did not have a real >1h event on the test server to visually confirm the H:MM:SS
branch — verified by code inspection (straightforward integer division/modulo, same pattern used elsewhere in
the codebase for HH:MM:SS formatting).

---

## 12. [High, security debt] `TrustConfig.applyAllowUntrusted()` violates this repo's own "no trust-all-certs" rule

**Context**: found during the 2026-07-09 inspection-report triage. `TrustConfig.kt`'s `applyAllowUntrusted()`
implements an empty `X509TrustManager.checkClientTrusted`/`checkServerTrusted` + an always-true hostname
verifier — exactly what root `CLAUDE.md` rule #5 ("No trust-all-certs toggle") and `core/network/CLAUDE.md`'s
"Forbidden" list explicitly ban, and it contradicts its own doc comment ("NEVER expose a trust everything
toggle"). It's wired to the live "Allow untrusted certs" toggle in `ServerFormSheet`, and the user's own test
server currently has it turned on.

**Why it's still open**: the documented, compliant replacement (pinned-PEM cert via
`CredentialStore.setPinnedCert`) already exists in storage but has **zero UI** to invoke it — removing the
trust-all toggle today would break the user's active connection with no fallback path. User's explicit decision
at the time: leave as-is, log as known debt, don't touch it yet.

**Definition of done**: build the pinned-cert import UI (paste/import a PEM, store via
`CredentialStore.setPinnedCert`), verify a self-signed server can connect with it, *then* remove
`applyAllowUntrusted()` entirely.

---

## 13. [Medium, tech debt] `RunBlocking` in `FrigateClient.kt`'s session-cookie/client-construction path — RESOLVED

**This was the exact cause of issue #22** (the Events-screen ANR) — confirming the "check whether it's actually
bridging a synchronous OkHttp SPI" question below the hard way, via a live-reproduced ANR and bugreport stack
trace. Answer: `SessionCookieJar.ensureLoaded()`'s `runBlocking` legitimately bridges the synchronous
`CookieJar.loadForRequest`/`saveFromResponse` SPI, but was *also* reachable through `FrigateClient.hasSessionCookie()`
— a plain suspend call with no such protection, which could (and did) run on the main thread. Fixed by wrapping
that specific call in `withContext(Dispatchers.IO)`. See #22 for the full root-cause writeup and verification.

<details>
<summary>Original notes (superseded by the above)</summary>

**Context**: flagged by `core/network/CLAUDE.md` as forbidden outside an interceptor (the equivalent
`runBlocking` in `buildClient()` was already found and removed in the "401 auto-refresh + runBlocking cleanup"
session — this is a *different*, still-present instance in the session-cookie loading path). Didn't fit a
"wrap up and commit" pass when found.

**Definition of done**: confirm the call site, check whether it's actually bridging a synchronous OkHttp SPI
(in which case it's legitimate, per the precedent already documented for `PerServerAuthInterceptor`/
`SessionCookieJar`) or whether it can become a plain `suspend fun` like `buildClient()`/`ensureFor` did.

</details>

---

## 14. [Low, investigate] Possibly-dead / half-wired code from Task B and elsewhere — RESOLVED

Audited and acted on each item individually:

- `CameraStreamState.Offline(reason)` — confirmed dead (`.reason` never read at the one place `Offline` is
  pattern-matched). **Deleted** — `Offline` is now a parameterless `object`; fixed the one construction site in
  `LiveStreamState.kt`.
- `RtspLiveTile`/`WebRtcLiveTile`'s `snapshotCachedAt` — confirmed dead (passed in, never read in either tile
  body). **Deleted** the parameter from both composables and their two call sites in `CamerasScreen.kt`.
- `SnapshotLiveTile`'s `showLastImageWhileLoading` — split verdict. The Settings UI toggle and DataStore key are
  real and genuinely drive RTSP/WebRTC tile behavior; only `SnapshotLiveTile` itself ignores it, because that
  tile already always shows the last frame via its own `lastPainter` Coil-placeholder mechanism (the "no-blink"
  design from `project_state.md`) — disabling that would reintroduce blink, so this isn't a bug to fix, it's a
  parameter that never applied to this tile. **Deleted** the unused param from `SnapshotLiveTile` + its call
  site, added a comment explaining why Snapshot mode doesn't take the setting.
- `EventsViewModel.delete`/`toggleRetain` — confirmed unreferenced by any UI. **Left in place**, intentionally —
  matches the "could be scaffolded for a future swipe/long-press menu" read from the original triage. Added a
  code comment marking it as deliberate scaffolding (also flags that the swallowed `else -> {}` error branches
  need fixing whenever that UI actually ships).
- `WifiMonitor.isEmulator()` — confirmed fully dead (private, zero call sites even within its own file).
  **Deleted.**
- `ApiResult.getOrNull()` — confirmed dead (all real `.getOrNull()` call sites app-wide are `kotlin.Result`, not
  this `ApiResult` extension). **Deleted.**
- `FrigateClient.activeServerFlow`/`setActive` — confirmed dead and redundant with the real active-server
  mechanism (`ServerRepository.setActive`/`activeServer()`, used everywhere else). **Deleted**, and fixed
  `FrigateClient`'s class-level KDoc, which had been referencing a nonexistent `[active]` symbol (stale doc
  pointing at this dead mechanism).

Verified on emulator after the cleanup: Cameras grid loads, all four tiles reach `Live` with the correct
recording-red dot — no regression. `./gradlew :app:compileDebugKotlin` clean throughout.

---

## 15. FIXED (scoped down), verified on emulator — Export/import settings

**Context**: flagged by the user as "fairly simple" (`project_state.md` "Future ideas"), not scoped yet. Would
dump `UserSettingsRepository`'s DataStore to a shareable file and restore it.

**Scoping decision**: explicitly limited to app preferences only — servers and credentials are never included
(separate system: `ServerRepository`/`CredentialStore`, a different trust boundary).

**Fix**: new "Backup & Restore" settings page (`BackupSettingsScreen.kt`), reachable from the Settings root list.
Export uses SAF `ActivityResultContracts.CreateDocument("application/json")` to write
`UserSettingsRepository.exportPreferencesJson()`'s output (a `{"version":1,"preferences":{...}}` JSON built from
a versioned `EXPORTABLE_KEYS` manifest — every real preference key except `KEY_LAST_KNOWN_CAMERA_NAMES`). Import
uses SAF `ActivityResultContracts.OpenDocument()`, reads the file, and calls
`UserSettingsRepository.importPreferencesJson()`, which validates the version and writes each key back via
`DataStore.edit`, returning false on any validation failure (surfaced as an "Import failed" toast).

**Verification (emulator)**: exported settings → pulled and inspected the file, confirmed correct JSON structure
and no server/credential data present → hand-edited `camera_grid_columns_v1` from `1` to `3` → imported the
modified file → got "Settings imported" toast → navigated to Settings → Cameras View and confirmed "Grid
columns" now reads "3x", proving the import round-trip genuinely writes values rather than just showing a
success toast. Pixel 8 testing deferred per user request (not needed for a DataStore-only feature with no
device-specific behavior).

---

## 16. FIXED, verified on emulator — Recent-events side panel next to live view

**Context**: from the 2026-07-10 live-view feature wishlist (reference: `screenshots/todo_005.png`), not built.
A panel next to (not replacing) the live view showing a scrollable "Recent Events" list — thumbnail, label,
timestamp, duration per row — with filter and sort/list-view toggle icons.

**Fix**: built together with #17 (same panel, two modes) — see #17 below for the shared implementation notes.
Per user direction, doesn't recreate the mockup pixel-for-pixel; uses this app's own Material3 components
(`SingleChoiceSegmentedButtonRow` for the List/Timeline toggle, `Card` rows, a label-filter `DropdownMenu`).

---

## 17. FIXED, verified on emulator — Timeline-strip side panel next to live view

**Context**: same wishlist (`screenshots/todo_006.png`), not built. Same side-panel layout as #16 but with a
compact vertical timeline strip (event markers + scrubber) instead of a flat list, toggled via an icon. Could
reuse `TimelineRenderer.kt`, which already implements this exact kind of strip for the Events screen.

**Fix**: new `LiveEventsPanel.kt` (`feature/cameras`), shown next to the live view only in fullscreen landscape
(`FocusedTile` now wraps its video `Box` in a `Row` + the panel when `isFullScreen && isLandscape` — portrait
fullscreen and the non-fullscreen card view are unchanged). List mode fetches one `events(camera, limit=25)` call
eagerly on mount; Timeline mode's three calls (`review`/`recordingGaps`/`motionActivity`) are fetched lazily —
only the first time the user switches to Timeline, and again on zoom — so a viewer who never opens Timeline
doesn't pay for it. The timeline strip reuses `feature/events/TimelineRenderer.kt`'s internal
`drawActivityTimeline` directly (same severity-colored waveform as the Events screen's own timeline), just
without its grid-scroll sync (there's no events grid alongside this panel) — tap-to-scrub plus zoom in/out only.
Tapping any List row or the "View all events" button calls the same `onNavigateToEvents(camera, null, null)`
already used by the grid tiles' existing swipe-left recent-event panel, for consistency.

**Verified live on the emulator**: rotated to landscape while ch1 was focused (app auto-entered fullscreen per
the existing `autoLandscapeOnStream` behavior) — panel appeared showing 3 real "Person detected" events with
thumbnails/bounding-box overlays, times, and durations. Toggled to Timeline — same real review-segment/motion
waveform as the Events screen rendered correctly with a live scrubber and time pill. Filter dropdown correctly
showed "All"/"Person" (the only label present on the test server's events for this camera). "View all events"
correctly navigated to the Events tab filtered to ch1. Rotated back to portrait — panel correctly disappears,
grid view unaffected.

---

## 18. FIXED, verified on emulator — Settings visual redesign extended to remaining sub-pages

**Context**: the card-per-row + bottom-sheet-picker + section-header redesign (2026-07-10, matched against
PixelPlayer reference screenshots) was applied to Appearance, Cameras View, Streaming, and Events. **Deliberately
not applied** to Servers (custom card list + form sheet), Advanced/Device Capabilities/About (custom stat/card
layouts, don't use the shared `SettingRow` component), or Notifications/Downloads (see #19 — still placeholders,
left untouched since there's no real content to restyle yet).

**Fix**: extended the same `SettingsSectionHeader`/`SettingsCard`/`SwitchSetting`/`ActionSetting` vocabulary to
the four remaining real sub-pages:
- **About**: single "App" section — app name/version in an icon+`SettingsCard` row, description and credits each
  in their own card (previously bare `Text` with no cards at all).
- **Advanced**: "System stats" header; the old ad-hoc `Checkbox` + `Button` row became a `SwitchSetting`
  ("Auto-refresh") + `ActionSetting` ("Refresh now"), and the loading/error states are now wrapped in
  `SettingsCard`. `SystemStatsView.kt`'s own "Detectors"/"Cameras" labels switched from plain `labelLarge` text to
  `SettingsSectionHeader`, and all of its `Card`s (stat tiles + detector/camera list) now use
  `MaterialTheme.shapes.large` + `surfaceContainer` to match every other settings card in the app.
- **Device Capabilities**: "Audio"/"Detected outputs"/"Codec support" section headers; the capability-stat grid's
  `Card`s and the previously card-less "Detected outputs" `ListItem` list and Media3/codec footer are now styled
  consistently (`ListItemDefaults.colors(containerColor = Color.Transparent)` so the `ListItem`s sit inside a
  `SettingsCard` without a color clash).
- **Servers**: added a "Servers" section header and restyled `ServerRow` to use `SettingsCard` directly (leading
  `Dns` icon, title/subtitle stack) instead of a bare `Card`; the "Add server" `Button` became an `ActionSetting`
  row. The `ServerFormSheet` modal itself (already partly using `SwitchSetting`) was left alone — it's a
  different UI surface (a bottom-sheet form, not a page-level settings list), consistent with the original scope
  note that Servers' *form sheet* wasn't part of this redesign, only the page-level list chrome around it.

**Verified live on the emulator** (Pixel 8 not connected this session): navigated to all four pages —
Servers (section header + icon'd card list + Add-server `ActionSetting`), Advanced (Auto-refresh switch, Refresh
now action, live CPU/GPU/RAM/Uptime stat cards, Detectors/Cameras section headers all rendering correctly),
Device Capabilities (Audio stat-card grid, Detected outputs card list, Codec support card), About (App section
card + description/credits cards) — all match the Appearance/Streaming/Events look, no regressions.
`./gradlew :app:compileDebugKotlin` and `:app:testDebugUnitTest` both clean.

---

## 19. [Medium, product decision] Notifications and Downloads settings are placeholders

**Context**: `NotificationsSettingsScreen.kt` and `DownloadsSettingsScreen.kt` exist as nav destinations but have
no real preferences behind them — no notification prefs or download prefs exist anywhere in the app yet.

**Definition of done**: needs a product decision on scope before implementation — what should actually be
configurable here (e.g. push-notification triggers/quiet hours, downloaded-clip storage location/retention)?

---

## 20. [Low, feature] Recording indicator dot should reflect active-event state — FIXED, verified on emulator

**Context**: from the fullscreen live-view redesign wishlist — the "● LIVE" dot in fullscreen chrome is a static
pulsing green, not aware of whether an event is actively ongoing on that camera right now (the reference app's
dot turns red during an active event). Originally skipped believing `CamerasViewModel`'s `recentEvents` only
tracked the *most recent* event per camera with no "is one ongoing right now" signal cheaply available.

**That blocker turned out to be stale.** `FrigateEvent.endTime: Double? = null` was already nullable (Frigate's
own convention for "not closed yet"), and `fetchRecentEvent()` already fetched exactly the right data
(`events(camera, limit=1)`) — it just cached the result forever instead of re-checking periodically.

**Fix**: `CamerasViewModel.setFocusedCamera(name)` starts/stops a poll scoped to the one camera currently focused
in fullscreen (not every grid tile — matches the "a feed being viewed now" framing), re-checking
`events(camera, limit=1)`'s `endTime` every 15s (`EVENT_POLL_INTERVAL_MS`, TODO-flagged for a future Developer
Options polling-rate control) and populating `activeEventCameraNames`. `LiveIndicatorDot` gained a third state:
solid blue (no pulse) when the focused camera has an open event — on top of the existing pulsing green (live)
and pulsing red (`#25`'s recording-enabled). Verified live on the emulator: poll fires every ~15s while
fullscreen (confirmed via logcat), stops immediately on exit, no crashes. Not yet verified on the real Pixel 8
(deferred per user request) or against a genuinely open event (didn't force one on the test server).

---

## 22. [High] Reproducible ANR on the Events screen — FIXED, verified on real Pixel 8

**Original hypothesis was wrong.** First seen while verifying fix #2 (the auto-refresh traffic loop); initially
suspected to be an artifact of heavy CPU load from the ADB automation used for that verification pass, since
plain logcat showed only "Input dispatching timed out" with no blocked-thread detail. A follow-up session
reproduced it live on the Pixel 8 (cold launch → tap Events → freeze, `Waited 5001-5005ms for MotionEvent`) and
pulled a full `adb bugreport`, which — unlike plain logcat — includes `/data/anr/anr_*` with the actual per-thread
Java stack trace. That made the real cause unambiguous:

```
"main" prio=5 tid=1 TimedWaiting
  at jdk.internal.misc.Unsafe.park(Native method)
  at kotlinx.coroutines.BlockingCoroutine.joinBlocking(Builders.kt:57)
  ...
  at net.triton.frigateviewer.core.network.SessionCookieJar.ensureLoaded(FrigateClient.kt:266)
  - locked <@addr=0x36acfa8> (a java.lang.Object)
  at net.triton.frigateviewer.core.network.SessionCookieJar.hasCookies(FrigateClient.kt:314)
  at net.triton.frigateviewer.core.network.FrigateClient.hasSessionCookie(FrigateClient.kt:86)
  at net.triton.frigateviewer.core.data.FrigateRepository.reloginIfNecessary(FrigateRepository.kt:60)
  ...
```

**Root cause**: `SessionCookieJar.ensureLoaded()` intentionally uses `runBlocking { credentialStore.cookies(...) }`
to bridge into `CredentialStore`'s suspend API — legitimate when reached through OkHttp's own synchronous
`CookieJar.loadForRequest`/`saveFromResponse` SPI (called from OkHttp's own background dispatch threads, per the
documented exception in `core/network/CLAUDE.md`). But `hasCookies()` is *also* called directly from
`FrigateClient.hasSessionCookie()`, which `FrigateRepository.reloginIfNecessary()` calls on **every** non-cached
API request — and that call chain runs on whatever dispatcher the caller's coroutine is on. Since ViewModels'
`viewModelScope.launch { }` defaults to `Dispatchers.Main.immediate` and nothing downstream switched off it, the
`runBlocking`-wrapped Keystore-backed cookie decrypt ran directly on the **main thread**.

**Why "all of a sudden" after the cookie code had been there a while — timeline, confirmed via `git log`:**
- **2026-07-09, commit `f81b95f`** ("implement per-server Frigate network client and WebRTC live view
  architecture") introduced both `SessionCookieJar` and `FrigateClient.hasSessionCookie()` together — this exact
  hazard (two different call paths into the same `runBlocking`-guarded `ensureLoaded()`, only one of them safe)
  has existed since day one of that architecture, not something newly broken this session.
- **2026-07-10, commit `0900678`** ("wire TokenRefreshAuthenticator and remove an unnecessary runBlocking") did a
  targeted review of this exact file for forbidden `runBlocking` usage per `core/network/CLAUDE.md`'s rule, and
  correctly removed a genuinely-unnecessary one in `buildClient()`. Its own commit message reasons about
  `SessionCookieJar`'s `runBlocking` too, concluding it's fine because it "bridge[s] a synchronous OkHttp SPI" —
  true for `loadForRequest`/`saveFromResponse`, but that review didn't separately notice `hasCookies()` is *also*
  reachable through `hasSessionCookie()`, a plain suspend call with no such protection. Half right, incomplete.
- **This session**: verifying the Wi-Fi and auto-refresh-traffic fixes involved an unusually large number of
  back-to-back `am force-stop` + cold-relaunch cycles on the same physical device in a short window (a dozen-plus
  across the whole session), plus concurrent CPU load from the ADB screenshot/tap automation itself. The
  main-thread block was very likely always there on a cold cache (freshly launched process, cookie not yet loaded
  into memory) — just normally fast enough (well under a second) not to be noticed. Repeated rapid Keystore-backed
  decrypts under real device load is a plausible trigger for crossing the 5-second ANR threshold specifically
  during this kind of intensive same-session testing, rather than in ordinary once-a-day app usage.

**Fix**: `FrigateClient.hasSessionCookie()` now wraps just the `hasCookies()` call in
`withContext(Dispatchers.IO)`, so the blocking decrypt always runs on the IO dispatcher's thread pool — the same
trade-off already accepted for `PerServerAuthInterceptor`'s legitimate `runBlocking` use — never on whatever
thread happened to call it. `SessionCookieJar`'s internal locking/loading logic is untouched.

**Verified on real Pixel 8**: 3 independent cold-start reproductions (force-stop → launch → tap Events
immediately) after installing the fix — all fully responsive, no ANR, confirmed via a fresh `adb logcat` sweep
showing zero `ANR in`/`Input dispatching timed out` entries across all three attempts.

This also closes the still-open tech-debt item #13 below (`RunBlocking` in `FrigateClient.kt`'s session-cookie
path) — it was this exact function, now fixed.

---

## 21. DROPPED — Full testTag sweep

**Context**: a "key elements only" pass (2026-07-10, explicit user scope choice) added `testTag`s to Settings
nav rows, Events filter button + event cards, camera grid tiles, and the fullscreen live-view chrome. **Still
untagged**: individual form fields inside `ServerFormSheet` and other settings sub-pages, `FilterSheet`'s
internal checkboxes, and dialog buttons.

**2026-07-11: dropped, not just deferred.** Re-examined the original motivation — the point was never "wire up
for an eventual instrumented-test suite," it was to let AI-driven verification look elements up by tag instead of
screenshot-and-tap-coordinate guessing. In practice, every session's live-device/emulator verification (including
this one) has worked fine with plain screenshots + computed tap coordinates — the need never actually materialized
across many sessions of exactly this kind of verification work. User's call: since it hasn't been needed, it
doesn't need to stay on the list. Not reopening unless a concrete pain point shows up.
